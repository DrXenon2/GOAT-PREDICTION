import React from 'react';

interface cardProps {
  // Props interface
}

export const card: React.FC<cardProps> = () => {
  return (
    <div>
      <h1>card</h1>
    </div>
  );
};

export default card;
