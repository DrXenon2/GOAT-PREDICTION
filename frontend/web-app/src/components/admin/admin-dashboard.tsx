import React from 'react';

interface admin-dashboardProps {
  // Props interface
}

export const admin-dashboard: React.FC<admin-dashboardProps> = () => {
  return (
    <div>
      <h1>admin-dashboard</h1>
    </div>
  );
};

export default admin-dashboard;
