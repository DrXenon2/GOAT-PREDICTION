import React from 'react';

interface filter-formProps {
  // Props interface
}

export const filter-form: React.FC<filter-formProps> = () => {
  return (
    <div>
      <h1>filter-form</h1>
    </div>
  );
};

export default filter-form;
