import React from 'react';

interface notification-listProps {
  // Props interface
}

export const notification-list: React.FC<notification-listProps> = () => {
  return (
    <div>
      <h1>notification-list</h1>
    </div>
  );
};

export default notification-list;
