import React from 'react';

interface match-cardProps {
  // Props interface
}

export const match-card: React.FC<match-cardProps> = () => {
  return (
    <div>
      <h1>match-card</h1>
    </div>
  );
};

export default match-card;
