import React from 'react';

interface AppProps {
  // Props interface
}

export const App: React.FC<AppProps> = () => {
  return (
    <div>
      <h1>App</h1>
    </div>
  );
};

export default App;
